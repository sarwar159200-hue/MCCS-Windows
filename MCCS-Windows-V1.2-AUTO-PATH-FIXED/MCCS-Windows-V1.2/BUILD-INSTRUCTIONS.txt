MCCS WINDOWS V1.2 - AUTO PATH FIX

This version removes the hard-coded GitHub folder path that caused:
Cannot find path ... MCCS-Windows-V1-Build-Package/MCCS-Windows-V1

The workflow now searches the repository automatically for the MCCS Windows app
by finding package.json beside src-tauri/Cargo.toml.

Use EXACT-main.yml as the complete content of:
.github/workflows/main.yml

Then:
GitHub > Actions > Build MCCS Windows V1 > Run workflow.

Expected artifacts:
- MCCS-Windows-V1-x64
- MCCS-Windows-V1-x86

The exact Miran Energy icon.ico remains included.
The application continues to open the live MCCS production website, so normal
website deployments automatically appear in the installed Windows application.
